#include <CUnit/CUnit.h>
#include "test_gql_matcher.h"

// Placeholder for matcher tests
int add_matcher_tests(void) {
    // For now, just return success - we'll add actual tests later
    return 0;
}